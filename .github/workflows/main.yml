name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         os: [ openbsd-6.9, netbsd-9.2 ]
    steps:
    - run: df -h
    - run: |
        dpkg-query -Wf '${Installed-Size}\t${Package}\n' | sort -rh | head -25 | cut -f 2 | sudo xargs apt-get --yes autoremove 
        sudo apt-get clean
    - run: df -h
#     - uses: mario-campos/emulate@main
#       with:
#         operating-system: ${{ matrix.os }}
#     - run: |
#         git clone https://github.com/mario-campos/qemu-test.git
#         cd qemu-test
#         make hello
#     - run: qemu-test/hello
